// This reducer would be useful for maintaining user details in real application

const initialState = {
    name: 'Himayan',
    id: 'user#1'
};

export default (state = initialState, action) => {
	switch (action.type) {
		default: {
			return state
		}
	}
}